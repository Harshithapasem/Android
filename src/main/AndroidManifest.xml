<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.harshita.tsfintern">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Education_Get"></activity>
        <activity android:name=".Personal_Get" />
        <activity android:name=".Professional_Get" />
        <activity android:name=".Personal_Act" />
        <activity android:name=".Education_Act" />
        <activity android:name=".Professional_Act" />
        <activity android:name=".Signup" />
        <activity android:name=".User_detail" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>